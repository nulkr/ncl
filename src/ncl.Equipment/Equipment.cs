namespace ncl
{
    namespace Equipment
    {
        //public enum Mode { StandBy, Manual, SemiAuto, Auto, Setup, Tenkey, Cycle };

        //public enum Status { Idle, Warning, Error, Initial, Run, Stop };

        //public enum Authority { Operator, Engineer, Administrator, Developer };
    }
}